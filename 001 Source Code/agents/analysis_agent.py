from langchain.prompts import PromptTemplate
from langchain_core.messages import HumanMessage

from multi_agent import GraphState, logger


def analyser_agent(state: GraphState) -> dict:
    """
    Analyser 에이전트: YOLO 탐지 로그와 이미지를 분석
    
    역할:
    - 멀티모달 입력(이미지 + 로그) 처리
    - 탐지된 객체, 위치, 화재/연기 정보 구조화
    - 증거 기반 분석 결과 생성
    
    참고: Multimodal RAG (2025)
    """
    logger.info("\n[ANALYSER] 분석 시작...")
    
    image, log = state["image"], state["log"]
    iteration = state.get("iteration", 0)
    feedback = state.get("feedback", "")
    router_decision = state.get("router_decision", "")
    
    # Router로부터 재분석 피드백이 있으면 반영
    if feedback and router_decision == "재분석":
        prompt = PromptTemplate.from_template("""
당신은 차량 화재 분석 전문가(Analyser)입니다.
Router로부터 재분석 요청을 받았습니다. 아래 피드백을 반영하여 로그와 이미지 정보를 바탕으로 차량 화재에 관해 구체적으로 작성하십시오.

[Router 피드백]
{feedback}

[분석 원칙]
1. 탐지된 객체(class)와 이미지 내 위치 관계를 기반으로 상황에 대해 구체적으로 서술
   • 좌표값이나 이미지 상 크기 수치를 그대로 언급하지 말고, 이미지 내 사물(예: 차량 앞부분, 엔진룸, 뒷좌석, 도로, 배경 건물 등)을 기준으로 표현합니다.
   • 여러 객체가 있을 경우 객체가 특정될 수 있도록 구체적인 표현을 사용하십시오.
   • 예시: "차량 전면부 하단에서 불꽃이 발생했으며, 연기가 차량 상단 방향으로 확산"

2. 객관성 유지
   • "심하게", "위험해 보임" 등 주관적 표현 배제
   • 관찰된 사실만 간결히 기술

3. 이미지와 로그의 일치 여부 확인
   • 탐지 결과가 시각적으로 타당한지 검증
   • 예시: "탐지 로그에서 'fire' 객체가 차량 보닛 부근에 위치하며, 실제 영상에서도 해당 위치에 불꽃이 관찰됨"
    
[분석 작성 원칙]
1.	차량 중심 분석
    •	차량의 구조적 구역(전면부, 엔진룸, 하부, 운전석, 뒷좌석, 후면, 지붕)을 기준으로 탐지 객체 위치를 기술합니다.
    •	차량이 화면에 여러 대 있을 경우, 가장 명확하게 탐지된 차량을 중심으로 기술합니다.
2.	화재 및 연기 분석
    •	불꽃(fire)의 위치, 크기, 확산 방향을 구체적으로 기술합니다.
    •	예시: “불꽃은 차량 하부 중앙부에서 시작되어 운전석 방향으로 확산됨.”
    •	연기(smoke)가 있을 경우, 발생 지점과 이동 방향을 함께 기술합니다.
    •	예시: “연기는 차량 후면에서 발생해 상단으로 확산됨.”
3.	차량 상태 서술
    •	차량 외관의 손상, 변색, 연소 흔적 등 관찰 가능한 사실을 기술합니다.
    •	예시: “차량 전면부 도장면에 그을음이 확인됨.”

[분석문 작성 형식]
1. 발생일시: 0000년 00월 00일 00시 00분
2. 세부내용:
    • 확인된 객체의 위치와 크기: ... 
    • 주변 구조물의 종류와 위치, 화재/연기/차량과의 거리: ... 
    • 차량 상태: ...
    • 화재/연기가 발견되는 차량 위치: ...
    • 화재/연기의 확산 방향: ... 
    • 화재/연기의 확산 가능성: ... 
    • 화재/연기의 위험도: ... 
    • 이후 대응 조치: ... 
    
[입력 데이터]
- YOLO 탐지 로그: {log}


""")
        resp = llm.invoke([
            HumanMessage(
                content=[
                    {"type": "text", "text": prompt.format(log=log, feedback=feedback)},
                    {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{image}"}}
                ]
            )
        ])
    else:
        prompt = PromptTemplate.from_template("""
당신은 차량 화재 분석 전문가(Analyser)입니다.
YOLO 모델이 탐지한 로그와 이미지를 분석하여 구조화된 분석 결과를 생성하십시오. 로그와 이미지 정보를 바탕으로 최대한 구체적으로 작성하십시오.

[분석 원칙]
1. 탐지된 객체(class)와 이미지 내 위치 관계를 기반으로 상황에 대해 구체적으로 서술
   • 좌표값이나 이미지 상 크기 수치를 그대로 언급하지 말고, 이미지 내 사물(예: 차량 앞부분, 엔진룸, 뒷좌석, 도로, 배경 건물 등)을 기준으로 표현합니다.
   • 여러 객체가 있을 경우 객체가 특정될 수 있도록 구체적인 표현을 사용하십시오.
   • 예시: "차량 전면부 하단에서 불꽃이 발생했으며, 연기가 차량 상단 방향으로 확산"

2. 객관성 유지
   • "심하게", "위험해 보임" 등 주관적 표현 배제
   • 관찰된 사실만 간결히 기술

3. 이미지와 로그의 일치 여부 확인
   • 탐지 결과가 시각적으로 타당한지 검증
   • 예시: "탐지 로그에서 'fire' 객체가 차량 보닛 부근에 위치하며, 실제 영상에서도 해당 위치에 불꽃이 관찰됨"
    
[분석 작성 원칙]
1.	차량 중심 분석
    •	차량의 구조적 구역(전면부, 엔진룸, 하부, 운전석, 뒷좌석, 후면, 지붕)을 기준으로 탐지 객체 위치를 기술합니다.
    •	차량이 화면에 여러 대 있을 경우, 가장 명확하게 탐지된 차량을 중심으로 기술합니다.
2.	화재 및 연기 분석
    •	불꽃(fire)의 위치, 크기, 확산 방향을 구체적으로 기술합니다.
    •	예시: “불꽃은 차량 하부 중앙부에서 시작되어 운전석 방향으로 확산됨.”
    •	연기(smoke)가 있을 경우, 발생 지점과 이동 방향을 함께 기술합니다.
    •	예시: “연기는 차량 후면에서 발생해 상단으로 확산됨.”
3.	차량 상태 서술
    •	차량 외관의 손상, 변색, 연소 흔적 등 관찰 가능한 사실을 기술합니다.
    •	예시: “차량 전면부 도장면에 그을음이 확인됨.”

[분석문 작성 형식]
1. 발생일시: 0000년 00월 00일 00시 00분
2. 세부내용: (아래의 내용을 포함하여 구체적으로 서술하시오.)
    • 확인된 객체의 위치와 크기: ... 
    • 주변 구조물의 종류와 위치, 화재/연기/차량과의 거리: ... 
    • 화재/연기의 확산 방향: ... 
    • 화재/연기의 확산 가능성: ...
    • 이후 대응 조치: ... 
    
    
[입력 데이터]
- YOLO 탐지 로그: {log}
""")
        
        resp = llm.invoke([
            HumanMessage(
                content=[
                    {"type": "text", "text": prompt.format(log=log)},
                    {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{image}"}}
                ]
            )
        ])
    
    logger.info(f"[ANALYSER] 분석 완료 (반복: {iteration + 1}회)")
    logger.info(f"분석 결과:\n{resp.content}\n")
    
    return {"analysis": resp.content, "iteration": iteration + 1}